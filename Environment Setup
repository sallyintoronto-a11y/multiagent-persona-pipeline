# Library Installation
!pip -q install openai langchain langchain-openai langgraph

# API Key Configuration
from google.colab import drive
drive.mount('/content/drive')

# Colab Secrets → env (fallback only if needed)
try:
    from google.colab import userdata
    os.environ.setdefault("OPENAI_API_KEY", userdata.get("OPENAI_API_KEY") or "")
except Exception:
    pass

client = OpenAI()  # Reads automatically from env
